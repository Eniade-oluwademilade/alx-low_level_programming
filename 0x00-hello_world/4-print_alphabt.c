#include <stdio.h>
/**printing alphabets except q & e
 * return: 0
 */
int main(void)
{
	char alpha;

	for((alpha >= 'a'; alpha <= 'z'; alpha++) && !(q && ))
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
