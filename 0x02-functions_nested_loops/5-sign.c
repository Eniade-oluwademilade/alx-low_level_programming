#include "main.h"
/**
 * main
 * printing sign of a number
 * Return: 1 if positive, 0 if zero, -1 if negative
 */
int main(void)
{
int print_sign(int n)
if (n > 0)
	_putchar("+\n");
return (1);
else if (n = 0)
	_putchar("0\n");
	return (0);
else
	_putchar("-\n");
	return (-1);
