#include "main.h"
/**
 * _strchr - locates char in string
                               - returns pointer to first occurence of char c in s else NULL if nil
 * Return: pointer to first occurence of c in s else NULL if none.
 */
char *_strchr(char *s, char c)
{

}
